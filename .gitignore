.streamlit/secrets.toml
__pycache__/
*.pyc
.DS_Store

# Respondent data stays OUT of the public GitHub repo — it lives in your private
# Hugging Face dataset instead (see README). form.xlsx is just the questionnaire
# structure (no respondent data) so it's fine to keep in git as a fallback.
data/survey_data.csv
